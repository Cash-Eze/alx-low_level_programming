#include <stdio.h>
#include "main.h"
#include <string.h>

/**
 * _strlen - a function to get the length of a string 
 * @*s: string to be passed 
 * Return: Always 0
 */

int _strlen(char *s)
{
char b;
*s = _strlen(b);

}
