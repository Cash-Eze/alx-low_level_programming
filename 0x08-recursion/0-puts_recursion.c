#include <stdio.h>
#include "main.h"
/**
 * _puts_recursion - A function to print a string
 * @s: string to print
 * Return: Nothing
 */

void _puts_recursion()
{
if (char *s)
{
_putchar(*s);
_put_recursion(s + 1);
}
else 
{
_putchar("\n");
}
}

